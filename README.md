# React + TypeScript + Vite

Template App for frontend Implementation using React - Vite and Typescript. We store JWTs locally (COOKIES). App uses SSL/TLS protocol with pem.

Score type in Question.ts should be adapted according to quiz's needs
